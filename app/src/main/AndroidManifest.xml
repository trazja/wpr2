<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tryhut.wpr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeWithNoBar">
        <activity android:name=".PlacesMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashScreenActivity" />
        <activity android:name=".PlacesActivity" />
        <activity android:name=".ViewpointsActivity" />
        <activity android:name=".ItActivity" />
        <activity android:name=".FoodtruckActivity" />
        <activity android:name=".WcActivity" />
        <activity
            android:name=".ServicesActivity"
            android:label="@string/places_name"
            android:theme="@style/AppTheme.ActionBar" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>